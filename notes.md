# Create DataFrame
###pd.DataFrame(data)

# Select column
###df['Age']

# Multiple columns
###df[['Name', 'Age']]

# Filter
###df[df['Age'] > 25]

# Rename
###df.rename(columns={'old':'new'})

# Sort
###df.sort_values(by='Salary')
